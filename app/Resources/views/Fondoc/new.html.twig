{% extends "PFEDashBundle::layout.html.twig" %}

{% block title %}
    CRUD - {{ parent() }}
{% endblock %}

{% block body -%}
    <div class="row card-panel wtitle top-teal col s11 cols2 crudcard">
        <h5 class="card-title">Fondoc creation</h5>
        {{ form_start(form) }}
        <div class="row">
            <div class="input-field col s6">
                {{ form_widget(form) }}
                  <input type="submit" value="Create" />
            </div>
           
           
           
            {{ form_end(form) }}

        </div>
         <a href="{{ path('pfe_saisi_fondoc') }}" class="btn blue-grey">
                <i class="mdi-action-view-headline left"></i> Retour à la liste
            </a>
    </div>
{% endblock %}

{% extends "PFEDashBundle::layout.html.twig" %}

{% block title %}
    CRUD - {{ parent() }}
{% endblock %}

{% block body -%}
    <div class="row card-panel wtitle top-teal col s11 cols2 crudcard">
        <h5 class="card-title">Equipement edit</h5>
        {{ form_start(editForm) }}
        <div class="row">
          
            <div class="input-field col s4">
                {{ form_row(editForm.nom) }}
            </div>
            <div class="input-field col s4">
               
            </div>
        </div>
        <div class="row">
            <div class="input-field col s1"><button type="submit" class="btn-floating btn-large waves-effect waves-light red"><i class="material-icons">edit</i></button></div>
            <div class="input-field col s3"><a href="{{ path('pfe_tutelle_index') }}" class="btn blue-grey"><i class="mdi-action-view-headline right"></i>Retour à la liste</a></div>
        </div>
        {{form_end(editForm)}}
     
    </div>
{% endblock %}
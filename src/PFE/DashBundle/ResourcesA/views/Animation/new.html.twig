{% extends "PFEDashBundle::layout.html.twig" %}

{% block title %}
    CRUD - {{ parent() }}
{% endblock %}

{% block body -%}
    <div class="row card-panel wtitle top-teal col s11 cols2 crudcard">
        <h5 class="card-title">Animation creation</h5>
        {{ form_start(form) }}
        <div class="row">
            <div class="input-field col s4">
                {{ form_row(form.typeanimation) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(form.occamenheb) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(form.publicvise) }}
            </div>
        </div>
        <div class="row">
            <div class="input-field col s4">
                {{ form_row(form.dateExposition) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(form.dateanimation) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(form.publicTotal) }}
            </div>
            {{ form_widget(form) }}
            <a href="{{ path('pfe_saisi_animation') }}" class="btn blue-grey">
                <i class="mdi-action-view-headline left"></i> Retour à la liste
            </a>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

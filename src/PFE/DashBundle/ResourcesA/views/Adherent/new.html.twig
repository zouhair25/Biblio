{% extends "PFEDashBundle::layout.html.twig" %}

{% block title %}
  CRUD - {{ parent() }}
{% endblock %}

{% block body -%}
<div class="row card-panel wtitle top-teal col s11 cols2 crudcard">
  <h5 class="card-title">Adherent creation</h5>
    {{ form_start(form) }}
		<div class="row">
	        <div class="input-field col s6">
		        {{ form_row(form.nom) }}
	        </div>
	        <div class="input-field col s6">
	          {{ form_row(form.prenom) }}
	        </div>
	      </div>
	     <div class="row">
	        <div class="input-field col s4">
	        	{{ form_row(form.dateNaissance) }}
	        </div>
	        <div class="input-field col s4">
	        	{{ form_row(form.sexe) }}
	        </div>
	        <div class="input-field col s4">
	        	{{ form_row(form.dateInscription) }}
	        </div>
    	{{ form_widget(form) }}
    	    <a href="{{ path('pfe_saisi_adherent') }}" class="btn blue-grey">
				<i class="mdi-action-view-headline left"></i> Retour à la liste
	        </a>
		{{ form_end(form) }}
		 </div>
</div>
{% endblock %}

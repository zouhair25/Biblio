{% extends "PFEDashBundle::layout.html.twig" %}

{% block title %}
    CRUD - {{ parent() }}
{% endblock %}

{% block body -%}
    <div class="row card-panel wtitle top-teal col s11 cols2 crudcard">
        <h5 class="card-title">Equipement edit</h5>
        {{ form_start(edit_form) }}
        <div class="row">
            <div class="input-field col s4">
                {{ form_row(edit_form.espace) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(edit_form.typeequipement) }}
            </div>
            <div class="input-field col s4">
         
            </div>
        </div>
        <div class="row">
            <div class="input-field col s4">
                {{ form_row(edit_form.nombre) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(edit_form.nombre_endommage) }}
            </div>
            <div class="input-field col s4">
                {{ form_row(edit_form.nombre_nutilisable) }}
            </div>
            
            
              <div class="row">
            <div class="input-field col s1"><button type="submit" class="btn-floating btn-large waves-effect waves-light red"><i class="material-icons">edit</i></button></div>
            <div class="input-field col s3"><a href="{{ path('pfe_saisi_equipement') }}" class="btn blue-grey"><i class="mdi-action-view-headline right"></i>Retour à la liste</a></div>
        </div>
            {{ form_end(edit_form) }}
    
        </div>
    </div>
{% endblock %}